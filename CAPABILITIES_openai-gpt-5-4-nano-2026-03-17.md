# CAPABILITIES_openai-gpt-5-4-nano-2026-03-17

Ниже — что я (как помощник на базе *openai-gpt-5-4-nano-2026-03-17*) способен(на) сделать в репозитории **mlaa4ml/KaggleModelsRepo**, и как вижу свою роль в проекте.

---

## Какие изменения кода я могу вносить

1. **Добавление/изменение файлов в репозитории**
   - Могу создавать новые `.py`, `.md`, конфигурационные файлы (например, `requirements`, `yaml`, шаблоны), а также править существующие.
   - Могу структурировать изменения так, чтобы они соответствовали текущему стилю проекта (после просмотра исходников через `read_file`).

2. **Модификация пайплайнов обучения/инференса**
   - Переписывать или дополнять этапы: загрузка данных → препроцессинг → тренировка → валидация → сохранение артефактов.
   - Предлагать и внедрять улучшения качества/устойчивости (например: правильные сплиты, воспроизводимость, обработка пропусков, нормализация, работа с категориальными признаками и т.п.).

3. **Оптимизация производительности и воспроизводимости**
   - Добавлять детерминизм (seed), документировать параметры.
   - Ускорять там, где это уместно: батчи, кэширование признаков, избегание лишних преобразований.

4. **Улучшение конфигураций экспериментов**
   - Внедрять единый формат конфигураций (например, через словари/аргументы/конфиги), чтобы разные модели запускались одинаково.
   - Помогать с автоматизацией: генерация запусков, единый интерфейс для моделей, метрики.

5. **Интеграция с метриками и валидацией**
   - Добавлять отчётность: сохранение результатов, логирование гиперпараметров, отчеты по метрикам.
   - Помогать внедрять корректные схемы оценки (cross-validation / holdout) в рамках принятых практик проекта.

6. **Исправления багов и рефакторинг**
   - Могу локализовать логические проблемы по прочитанному коду.
   - Делать рефакторинг без изменения поведения, если нужно улучшить читаемость/поддерживаемость.

---

## Что я делаю «по процессу» при работе с этим проектом

1. **Сначала читаю существующий код и структуру**
   - Открываю ключевые файлы (тренировочные скрипты, утилиты, README, конфиги) и выявляю принятые соглашения.

2. **Предлагаю план изменений**
   - Формирую список правок: какие файлы затрону, почему, и как это влияет на обучение/инференс.

3. **Вношу изменения точечно**
   - Стараюсь минимизировать риск: короткие патчи, ясные диффы, согласованные нейминги.

4. **Пишу/обновляю документацию**
   - Добавляю заметки в `*.md` (как запускать модель, как устроена структура, какие данные нужны).

---

## Какую роль я вижу для себя в KaggleModelsRepo

- **Ассистент по разработке моделей и экспериментам.** Я помогаю быстро прототипировать и доводить до работающего состояния: от структуры до улучшений качества.
- **Архитектор документации.** Часто в таких репозиториях ценность — в воспроизводимости и понятных инструкциях; я помогаю привести модели к единому интерфейсу и описаниям.
- **«Сторож качества».** Я обращаю внимание на воспроизводимость, правильность препроцессинга и корректность оценки (чтобы метрики на Kaggle не были «случайными»).

---

## Чем я не могу (или не должен) заниматься

- **Не исполняю код и не запускаю эксперименты** напрямую (в рамках текущей среды доступны только чтение/запись файлов). Поэтому я опираюсь на статический анализ и лучшие практики.
- Не могу проверить реальный прогон на данных; вместо этого стараюсь предложить изменения так, чтобы они были логически корректны и легко проверяемы пользователем.

---

## Формат взаимодействия

- Если вы опишете задачу (например: «добавь новую модель/датасет», «почини препроцессинг», «обнови README»), я:
  1) прочитаю нужные файлы,
  2) предложу конкретные правки,
  3) внесу изменения в код/документацию.

---

Если нужно, я могу начать с просмотра структуры репозитория и ключевых файлов (скрипты обучения/инференса, README, каталог моделей) и предложить точечный план интеграции нужной модели.